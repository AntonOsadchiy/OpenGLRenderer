#include "Material.h"

//Material::Material(std::vector<std::string>&& diffuse, std::vector<std::string>&& specular, float shine )
//	:diffuse_maps(diffuse.size()), specular_maps(specular.size()), shininess(shine)
//{
//	for (int i = 0; i < diffuse.size(); i++)
//		diffuse_maps.at(i) = Texture2D{ diffuse[i] };
//	for (int i = 0; i < specular.size(); i++)
//		specular_maps.at(i) = Texture2D{ specular[i] };
//}